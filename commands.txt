To create a digital identity in Git - linking name and email to every commit.
git config --global user.name "name"
git config --global user.email "email"

//TO Create a file
echo - used to create a file

git init 
git add .
git commit -m ""
git push